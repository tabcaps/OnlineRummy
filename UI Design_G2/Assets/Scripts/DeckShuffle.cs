using UnityEngine;
using System.Collections;

public class DeckShuffle : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
		Debug.Log ("in Start()");
	}
	
	// Update is called once per frame
	void Update () {
	
		if(Input.GetMouseButtonDown(0))
		{
			Debug.Log("Mouse000000");
		}
	}
}
